# GitHub Scout Skill 设计思路文档

## 1. 项目背景

目标：开发一个用于调查 GitHub 用户公开项目情况的 AI Skill。

使用场景：

> 输入一个 GitHub 用户名，自动分析该用户公开 GitHub 活动、仓库和项目情况，帮助快速了解一个开发者正在做什么、技术方向、项目质量和贡献情况。

典型用途：

- 调查身边同学/开发者的 GitHub 公开项目

- 快速了解某人的技术栈

- 找出其最有价值的项目

- 判断项目复杂度和个人技术方向

- 为学习交流、招聘、合作提供参考

---

# 2. 需求规格

本章定义「做到什么程度算做完」。第 3～8 章描述**怎么做**，本章描述**做成什么**。

## 2.1 目标用户与使用场景

| 场景 | 使用者 | 核心问题 | 对调查的要求 |
|---|---|---|---|
| 自我复盘 | 开发者本人 | 我的 GitHub 对外呈现出什么形象？ | 结论要诚实，不粉饰 |
| 了解同学/同事 | 同龄开发者 | 他在做什么方向？有什么值得聊的项目？ | 要能找出真正的代表作 |
| 招聘初筛 | 技术面试官 | 简历上的项目是真做的还是教程抄的？ | 必须能区分原创与模板 |
| 合作评估 | 潜在协作者 | 这个人是否长期维护项目？靠谱吗？ | 重活跃度与维护连续性 |

四个场景共同的取舍依据：**宁可少列几个项目，也不能把教程仓库当成代表作。** 后续所有规则冲突时，以此为准。

## 2.2 功能需求（FR）

与第 6 章工作流程 Step 1～5 一一对应。

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| FR-1 | 用户解析 | 接受 username 或 profile URL，校验用户存在，获取基础 profile（简介、加入时间、关注数、公开仓库数） | M1 |
| FR-2 | 仓库枚举 | 列出全部公开仓库及元数据：语言、stars、forks、创建/更新时间、是否 fork、是否 archived、是否 template | M1 |
| FR-3 | 仓库过滤 | 识别并降权 fork、空仓库、教程/课程作业/脚手架模板，保留原创且有实质内容的项目 | M1 |
| FR-4 | 代表项目粗筛 | 按元数据可算的信号粗排，输出 `max(12, top_n×2)` 个候选。**这一步不做最终排名** | M1 |
| FR-5 | 主排序与深度分析 | 对候选取真实代码规模与贡献数据定出 Top N，再对 Top N 分析项目目的、技术栈、架构、复杂度 | M1 |
| FR-6 | 画像生成 | 按第 7 章统一格式输出开发者画像报告 | M1 |
| FR-7 | 优雅降级 | 无仓库 / 全是 fork / 部分数据不可得时，输出有依据的结论与明确的数据缺口说明，而非空报告或报错 | M1 |
| FR-8 | 参数化调节 | 支持调整 Top N 数量与分析深度 | M2 |

## 2.3 非功能需求（NFR）

| 编号 | 要求 | 指标 |
|---|---|---|
| NFR-1 | 调用预算 | 单次调查（含 Top 5 深度分析）控制在 80 次 API 调用内，典型约 66 次；仓库数超过 100 的用户先按元数据粗筛再深入 |
| NFR-2 | 耗时 | 典型用户（20～50 仓库）单次调查目标在数分钟内完成 |
| NFR-3 | 无凭证降级 | 无 API token 时退化为公开页面读取，功能范围收窄但仍需产出报告，并在报告中注明降级 |
| NFR-4 | 结论可复现 | 同一用户在短期内两次调查，Top 3 项目集合应一致；技术方向判断不应互相矛盾 |
| NFR-5 | 跨平台一致 | 同一用户在不同 adapter 上的 Top N 与复杂度评级应一致，差异仅允许出现在呈现细节 |
| NFR-6 | 数据边界 | 只使用无需授权即可访问的公开信息；不尝试访问私有仓库、邮箱等非公开数据 |
| NFR-7 | 结论可回溯 | 报告中每条判断都应能指向具体仓库、数据或时间区间，不输出无依据的评价 |

## 2.4 输入输出契约

这一节是 core 与 adapter 的分界线：**core 定义契约，adapter 决定怎么实现。**

### 输入

```
必填：
  username        GitHub 用户名，或完整 profile URL

可选：
  top_n           深度分析的项目数量，默认 5，范围 1～10
  depth           分析深度：quick（仅元数据）| standard（默认）| deep（含 commit 历史分析）
  focus           关注方向提示，例如 "AI"、"前端"，影响报告的侧重而非排序
```

### 输出

单份 Markdown 报告，章节固定（详见第 7 章），至少包含：基本信息、技术方向、代表项目、GitHub 活跃度、综合评价。

数据缺口必须显式声明，例如「该用户 commit 邮箱不公开，个人贡献占比无法核实」，不得以沉默略过。

## 2.5 验收标准

- [ ] 对一个有 20+ 公开仓库的真实用户，能在 NFR-1 预算内产出结构完整的报告
- [ ] Top 5 排序结果与人工判断基本一致，不出现明显误排
- [ ] 对以 fork 为主的用户，不产生虚高评价；fork 仓库不进入 Top 5
- [ ] 教程/课程作业类仓库被正确识别并降权
- [ ] 同一用户连续两次调查，Top 3 项目集合一致（NFR-4）
- [ ] 对仓库数为 0 或全为空仓库的用户，输出有意义的结论而非报错（FR-7）
- [ ] 报告中每条结论都可回溯到具体仓库或数据（NFR-7）
- [ ] 无 token 环境下仍能完成调查并注明降级（NFR-3）

## 2.6 非目标（Out of Scope）

明确**不做**的事情，防止范围蔓延：

- **不访问私有仓库**，不使用任何需要额外授权才能获取的数据
- **不做代码质量静态扫描**：不跑 linter，不算圈复杂度，不做安全审计。复杂度判断基于结构与规模的整体观察，不是代码审查
- **不对人做价值排名或打分**：输出的是技术画像，不是「这个人几分」。第 9 章的横向比较也只比方向与特征，不产出优劣结论
- **不爬取 GitHub 之外的平台**：不关联个人博客、社交账号、简历站点
- **不做实时监控**：一次调查是一次快照，不负责跟踪后续变化
- **不生成对个人的负面判断**：可以指出「仓库多为教程项目」这类事实，不引申为对个人能力的贬低

## 2.7 MVP 与迭代范围

| 阶段 | 范围 | 完成标志 |
|---|---|---|
| **M1** | FR-1～FR-7，core 三件套 + Claude adapter | 对真实用户端到端跑通，2.5 全部勾选 |
| **M2** | FR-8，ChatGPT adapter | 同一用户在两个平台上结论一致（NFR-5） |
| **M3** | 第 9 章「后续增强方向」的四项能力 | 逐项独立评估，不绑定发布 |

---

# 3. Skill 命名

推荐名称：

## GitHub Scout

Skill ID：

```
github-scout
```

含义：

- GitHub：明确数据来源

- Scout：侦察、探索、发现

相比：

- `github-analyzer`：偏代码分析，不突出用户调查

- `repo-analyzer`：只针对单个仓库

- `github-investigator`：语气偏调查取证

`github-scout` 更符合：

> 先发现一个人的 GitHub 世界，再深入分析重点项目。

---

# 4. 核心设计理念

采用两阶段分析模式：

```
GitHub User
    │
    ↓  阶段 1：Profile Scout
发现用户主要项目
    │
    ↓  阶段 2：Repository Analyzer
深入分析重点仓库
    │
    ↓
生成开发者画像
```

---

# 5. 基础能力组合

## 第一部分：GitHub Profile Analysis

来源思路：

`github-profile`

作用：

负责用户级扫描。

主要功能：

- 获取 GitHub 用户信息

- 获取公开仓库列表

- 获取最近活动

- 分析语言分布

- 查看 Stars / Forks

- 查看更新时间

- 判断 GitHub 活跃程度

输出：

例如：

```
用户：xxx
主要语言：Python / JavaScript / C++
最近一年主要方向：AI Agent / Web Development / Machine Learning
代表项目：project-A / project-B / project-C
```

---

## 第二部分：Repository Deep Analysis

来源思路：

`repo-analyzer`

作用：

负责单个项目深度分析。

主要功能：

- 分析项目结构

- 分析代码规模

- 分析技术架构

- 分析依赖

- 分析 README

- 分析 Git history

- 判断项目复杂度

输出：

例如：

```
项目：AI-Agent Framework
技术栈：Python / LangChain / FastAPI
项目特点：
  - 多模块架构
  - 有完整 README
  - 有持续 commit
  - 非简单 demo
复杂度：★★★★☆
```

---

# 6. 完整工作流程

## Step 1：输入用户

输入：

```
github username
```

例如：

```
username: xxx
```

---

## Step 2：扫描 Profile

获取：

- 用户简介

- 所有公开仓库

- 最近 commit

- 最近 PR

- Star 项目

- Fork 项目

---

## Step 3：过滤仓库

自动区分：

保留：

- 原创项目

- 长期维护项目

- 有 README 的项目

- 有持续 commit 的项目

降低权重：

- fork 项目

- 空仓库

- tutorial

- demo

- 课程模板

### 教程 / 模板项目识别

命中以下 **≥2 条**即判定为教程或模板项目：

- 仓库名含 `tutorial`、`learn`、`practice`、`demo`、`test`、`hello`、`homework`、`assignment`、课程名、训练营名
- README 明显来自课程模板或脚手架，未做实质修改
- commit 历史集中在 1～3 天内，且 message 多为 `update`、`commit`、`init` 等无信息量内容
- 目录结构与知名脚手架或教程仓库完全一致，无自有扩展
- 仓库由 template 生成，且生成后改动很少

判定为教程项目不代表该仓库无价值，只影响它是否能作为**代表作**参与排序。

---

## Step 4：选择重点项目

### 评分模型

总分 100 分，四个维度加权：

| 维度 | 权重 | 说明 |
|---|---|---|
| 技术价值 | 35 | 项目本身的工程含量 |
| 活跃程度 | 25 | 是否持续投入 |
| 个人贡献 | 25 | 是否本人主导 |
| 项目影响力 | 15 | 外部认可度 |

**关于权重的取舍：** 影响力（stars）只占 15 分，是刻意压低的。本 Skill 的主要场景是调查普通开发者与学生（见 2.1），这类用户绝大多数仓库 stars 为 0。若按常见做法把 stars 设为主导权重，所有候选项目会一起归零，排序完全失效。技术价值与个人贡献才是这个场景下真正有区分度的信号。

### 各维度计分细则

**技术价值（35 分）**

| 信号 | 判定 | 得分 |
|---|---|---|
| 代码规模 | 空仓库 / 仅 README | 0 |
| | < 500 行 | 3 |
| | 500 ～ 3,000 行 | 8 |
| | 3,000 ～ 15,000 行 | 12 |
| | > 15,000 行 | 15 |
| 架构复杂度 | 单文件脚本 | 0 |
| | 简单多文件，无分层 | 4 |
| | 有模块划分或分层设计 | 8 |
| | 多模块 + 测试 + CI | 12 |
| 技术栈难度 | 常见脚手架默认栈 | 0 |
| | 主流框架的常规应用 | 3 |
| | 需要专门领域知识（编译器、分布式、模型训练、系统编程等） | 8 |

**代码规模按各语言源码字节数总和折算**（约 30 字节 ≈ 1 行）。**不得用仓库体积（disk usage）估算**——它含图片与构建产物，实测偏差可达 4,200 倍，会让资源密集型仓库拿满分并挤掉真正的代表作。仓库体积只能用于判断「是否几乎为空」。

**commit 时间跨度取首末 commit 的时间差**，**不得用仓库创建时间到最近 push 的间隔代替**——实测单 commit 仓库因此被误判为长期维护并拿到满分。

**活跃程度（25 分）**

| 信号 | 判定 | 得分 |
|---|---|---|
| 最近 push 距今 | < 1 个月 | 10 |
| | 1 ～ 3 个月 | 8 |
| | 3 ～ 6 个月 | 5 |
| | 6 ～ 12 个月 | 2 |
| | > 12 个月 | 0 |
| commit 时间跨度 | < 1 周 | 0 |
| | 1 周 ～ 1 个月 | 3 |
| | 1 ～ 6 个月 | 6 |
| | > 6 个月 | 9 |
| commit 总数 | < 10 | 0 |
| | 10 ～ 49 | 3 |
| | 50 ～ 199 | 5 |
| | ≥ 200 | 6 |

**个人贡献（25 分）**

| 信号 | 判定 | 得分 |
|---|---|---|
| 本人 commit 占比 | < 20% | 0 |
| | 20% ～ 50% | 5 |
| | 50% ～ 80% | 9 |
| | > 80% | 12 |
| 是否 owner 且非 fork | 是 | 8 |
| | 否 | 0 |
| 是否持续维护 | commit 分布跨越 ≥3 个不同月份 | 5 |
| | 否 | 0 |

占比无法核实时（commit 邮箱不公开等），该项按 owner 身份保守估算，并在报告中标注为估算值。

**项目影响力（15 分）**

| Stars | 得分 |
|---|---|
| 0 | 0 |
| 1 ～ 9 | 3 |
| 10 ～ 49 | 7 |
| 50 ～ 199 | 11 |
| ≥ 200 | 15 |

Forks 与 Watchers 作为佐证不单独计分。若 forks 明显高于 stars，通常是课程作业或教程模板的特征，应触发 Step 3 的教程识别检查。

### 降权系数

对总分做乘法调整，多个条件命中时取最低系数：

| 条件 | 系数 |
|---|---|
| fork 且无实质自有 commit | ×0.1 |
| fork 但有显著自有改动（自有 commit ≥30 或有独立功能） | ×0.6 |
| 命中教程 / 模板判定 | ×0.3 |
| 空仓库或仅有 README | ×0.1 |
| 已 archived | ×0.8 |

### 时间窗口定义

统一口径，避免各处理解不一：

- **最近活跃**：最近一次 push 在 90 天内
- **长期维护**：commit 分布跨越 ≥3 个不同月份
- **近期方向**：最近 12 个月内有 commit 的仓库集合
- **技术成长轨迹**：按仓库创建年份分组统计

### 分阶段评分

评分需要的数据获取成本差异很大，因此排序分三阶段进行：

| 阶段 | 每仓库调用 | 可用数据 | 可计算 | 分制 | 产出 |
|---|---|---|---|---|---|
| **Step 4 粗筛** | 0 | 仅元数据 | 影响力 15 + 最近 push 10 + owner 身份 8 | 33 分制 | `max(12, top_n×2)` 个候选 |
| **Step 5a 主排序** | 2 | + 语言字节数、贡献者列表 | 追加 代码规模 15 + commit 数 6 + 本人占比 12 | 66 分制 | 最终 Top N |
| **Step 5b 深度分析** | 5 | + commit 首末时间、目录结构、依赖、README | 追加 跨度 9 + 持续维护 5 + 架构复杂度 12 + 技术栈难度 8 | 100 分制 | Top N 内部顺序、复杂度星级 |

**commit 跨度为什么放在 5b。** 取跨度每仓库需额外两次调用，12 个候选就是 24 次。实测表明加入跨度会改变 Top 5 的内部顺序，但**选出的仍是同一批项目**——它对「选谁」没有决定作用，对「谁排前面」才有。

**为什么粗筛要放宽到 12 个。** Step 4 只能算出 33 分，其余 67 分不可得。在这个分制上做最终淘汰会退化成大面积并列——实测某用户出现 6 个仓库精确同分，Top 5 的最后一名由排序的偶然顺序决定，直接破坏 NFR-4。

更严重的是**淘汰不可逆**：Step 5 的修正只能调整已入选者的顺序，救不回没入选的项目。实测中一个 1,108 stars 的近期专门领域工具，因周期短、体积小，在 33 分制下排在一批十年前的老项目之后而直接出局。

**任何阶段都不得用估算代理补当阶段拿不到的信号**，宁可让该项计 0 分。仓库体积不等于代码规模，创建时间不等于 commit 跨度——这两个代理都在实测中造成了严重误排。

**同分破平**：按 stars 降序、再按仓库名字典序，不得依赖数据返回的偶然顺序。

---

## Step 5a：主排序

对 Step 4 的候选逐个取真实数据——各语言源码字节数、贡献者列表及各自 commit 数——按 66 分制打分，定出最终 Top N。

**决定最终名单的是这一步，不是 Step 4。**

---

## Step 5b：深入分析 Top 项目

对 Step 5a 选出的 Top N 运行 repo analyzer。

分析：

- 项目目的

- 技术路线

- 架构设计

- 难度

- 实际贡献

这一步可调整 Top N 内部顺序，但不改变入选名单。

---

# 7. 最终输出格式

生成开发者画像：

```markdown
# GitHub Developer Report

## 基本信息
用户名：
加入时间：
公开仓库数：

## 技术方向
主要语言：
主要领域：

## 代表项目

### 1. Project A
用途：
技术栈：
复杂度：
个人贡献：

### 2. Project B
用途：
技术栈：
复杂度：
个人贡献：

## GitHub 活跃度
Commit：
PR：
最近活动：

## 综合评价
该开发者主要方向：
技术特征：
值得关注项目：

## 数据说明
本次调查的数据缺口与估算项：
```

### 复杂度星级对照

| 综合得分 | 星级 |
|---|---|
| 0 ～ 19 | ★☆☆☆☆ |
| 20 ～ 39 | ★★☆☆☆ |
| 40 ～ 59 | ★★★☆☆ |
| 60 ～ 79 | ★★★★☆ |
| 80 ～ 100 | ★★★★★ |

「数据说明」一节对应 NFR-7 与 FR-7：任何无法核实的数据都在这里显式列出，不允许静默略过。

---

# 8. 仓库架构：Core + Adapters（跨 Agent 设计）

## 8.1 为什么要拆分

GitHub Scout 的本质是一套**调查工作流**，而不是某个模型独有的能力：

```
GitHub 用户 → 获取公开信息 → 判断活跃仓库 → 筛选代表项目
→ 深入分析 → 判断技术栈/项目方向 → 生成用户画像
```

这条链路在任何 Agent 平台上都完全一致，真正有差异的只是**如何拿到数据**和**如何组织指令**。

因此不应该做成两个（或五个）彼此独立的项目，而应该做成：

> **一个项目、多个 Skill 版本** —— 共用核心方法论，只替换平台适配层。

预估拆分比例：**约 80% 核心逻辑共用，20% 平台适配**。

---

## 8.2 目录结构

初期（双平台）：

```text
github-scout/
├── core/
│   ├── methodology.md      # 调查方法论：两阶段流程
│   ├── repo-ranking.md     # 仓库筛选与评分规则
│   └── report-format.md    # 开发者画像输出格式
│
├── chatgpt/
│   └── SKILL.md
│
├── claude/
│   ├── SKILL.md
│   └── references/         # 构建产物，由 core 同步而来
│
├── scripts/
│   └── sync.ps1
│
└── README.md
```

后续扩展到更多平台时，收敛到 `adapters/`：

```text
adapters/
├── chatgpt/
├── claude/
├── codex/
├── cursor/
└── gemini/
```

新增一个平台 = 新增一个 adapter，而不是把整套逻辑重做一遍。

---

## 8.3 职责划分

| 层 | 内容 | 是否共用 |
|---|---|---|
| `core/methodology.md` | 调查方法（Step 1～5 完整流程） | ✅ 共用 |
| `core/repo-ranking.md` | 仓库筛选规则、项目评分维度 | ✅ 共用 |
| `core/report-format.md` | 开发者画像报告结构 | ✅ 共用 |
| adapter | 工具调用方式 | ❌ 平台特有 |
| adapter | 数据获取通道 | ❌ 平台特有 |
| adapter | Skill 指令格式与元数据 | ❌ 平台特有 |
| adapter | 输出呈现细节 | ❌ 平台特有 |

具体差异对照：

| 共用能力 | ChatGPT 版 | Claude 版 |
|---|---|---|
| 调查方法 | ChatGPT 工具调用方式 | Claude 工具调用方式 |
| 仓库筛选规则 | ChatGPT Skill 指令格式 | Claude Skill 指令格式 |
| 项目评分 | GitHub connector / Web 检索 | MCP / `gh` CLI / Bash |
| 输出格式 | ChatGPT-specific 呈现 | Claude-specific 呈现 |

---

## 8.4 设计约束

为保证 core 真正可复用，需要遵守几条约束：

- **core 不提工具名**：只描述"需要获取用户的公开仓库列表"，不写 `gh repo list` 或某个 connector 名称。
- **core 只定义输入/输出契约**：每一步说明"需要什么信息、产出什么结论"，具体怎么拿由 adapter 决定。
- **adapter 保持薄**：只做工具映射 + 平台指令包装，不复制粘贴方法论内容，通过引用 core 文件来复用。
- **评分规则集中在 core**：任何平台上对同一个用户的调查结论应当基本一致，避免各版本结论漂移。

---

## 8.5 分发方式：core 与 references 的关系

Claude Skill 按**目录自包含**分发。若 `claude/SKILL.md` 直接写 `../core/methodology.md`，skill 被复制或打包安装后跨目录引用会断链。

解法：

- `core/` 是唯一真源，**人只改这里**
- `scripts/sync.ps1` 把 `core/*.md` 同步到各 adapter 的 `references/`
- adapter 只引用自己目录内的 `references/*.md`，安装时自包含
- `references/` 视为构建产物，**不要手改**，改动会在下次同步时被覆盖

这样既保住「core 是唯一真源」，又满足各平台的分发要求。

---

## 8.6 项目定位

基于这一架构，仓库可以直接定位为**跨 Agent 的 GitHub Intelligence Skill**。

README 一句话定位：

> **GitHub Scout — AI-powered GitHub developer intelligence skill for ChatGPT and Claude.**

后续支持更多平台时，这句话只需扩展平台列表，定位本身不变。

---

# 9. 后续增强方向

## AI 项目识别

自动判断：

- 是否 AI 项目

- 是否使用 LLM

- 是否 Agent

- 是否科研方向

---

## 项目真实性分析

判断：

- 是否只是教程项目

- 是否大量复制模板

- 是否本人主要开发

- 是否长期维护

---

## 技术成长轨迹

分析：

```
2023: Python 基础项目
2024: Web 应用
2025: AI Agent
```

推测开发路线。

---

## 横向比较

未来可以支持：

```
Compare: User A vs User B

技术方向：
项目质量：
活跃程度：
科研潜力：
工程能力：
```

注意：横向比较只对比方向与特征，不产出优劣排名（见 2.6 非目标）。

---

# 10. 最终目标

打造一个：

> 输入 GitHub 用户名，即可快速生成该开发者公开技术画像的 AI Skill。

核心定位：

**GitHub Scout = 发现用户 + 分析项目 + 总结开发者能力。**
